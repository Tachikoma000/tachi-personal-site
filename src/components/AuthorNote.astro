---
interface Props {
  label: string;
  continueLabel?: string;
}
const { label, continueLabel } = Astro.props;
---
<div class="author-note">
  <div class="note-label">{label}</div>
  <slot />
  {continueLabel && <div class="note-continue">↓ {continueLabel}</div>}
</div>
